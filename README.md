## E-Commerce App API

This is a complete web **API** created using **ASP.NET Core**, **EF Core** and **SQLite**.
Users can do CRUD operations over Category and Product objects and save them to the SQLite database.

